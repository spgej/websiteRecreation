//Program for iterating through database to render
//most recent objects as header on main page
//WIP

const reverse = array.reverse();
const n = 5;
const slicedArray = reverse.slice(0, n);

for (var i = 0; i < 3; i++) {
  slicedArray.forEach((obj.length[i])=>{

  });
}
